
if ( $host ~ "^([^\.]+)\.([^\.]+)\.(([^\.]+)\.)?dev$"  ) {
     set $customer "$1";
     set $project "$2";
     set $store "$4";
}

if ( $store = "" ) {
#    set $store "default";
}

set $htdocs "htdocs";
if ( -e /data/$customer/$project/httpdocs ) {
    set $htdocs "httpdocs";
}
if ( -e /data/$customer/$project/public ) {
    set $htdocs "public";
}

location ~ ^/?\. {
    deny all;
}
