#!/usr/bin/env bash

#docker run -it --rm -v "${PWD}":"${PWD}" --net=host -w "${PWD}" originalbrownbear/php:7-cli-phpunit-xdebug phpunit "$@"
docker run -it --rm -v "${PWD}":"${PWD}" --net=host -w "${PWD}" maxlab/php7xdebug phpunit "$@"
